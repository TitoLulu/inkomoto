# World Bank / Socrata API (optional — increases rate limit from 1000 to unlimited req/hour)
# Register free at https://data.worldbank.org/
SOCRATA_APP_TOKEN=

# PostgreSQL
POSTGRES_USER=dataeng
POSTGRES_PASSWORD=dataeng_pass
POSTGRES_DB=analytics_db
AIRFLOW_DB=airflow

# ClickHouse
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=clickhouse_pass

# Airflow
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=admin_pass
# Run this command and paste the output here:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=CHANGE_ME_run_python_c_from_cryptography.fernet_import_Fernet_print_Fernet.generate_key
# Run this command and paste the output here:
#   python -c "import secrets; print(secrets.token_hex(32))"
AIRFLOW_SECRET_KEY=CHANGE_ME_run_python_c_import_secrets_print_secrets.token_hex_32

# Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin_pass
